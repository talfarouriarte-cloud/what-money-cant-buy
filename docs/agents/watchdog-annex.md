# Anexo de rol — Watchdog (what-money-cant-buy)

Los workflows `update.yml`, `new-season.yml` y `build-crests.yml` son de PRODUCTO, no de pipeline (ADR-004): no los vigiles, no los diagnostiques, no los relances. Operan sobre `main`; tu dominio es `develop`. Label de épica: `epica`.
