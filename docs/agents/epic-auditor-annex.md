# Anexo de rol — Epic Auditor (what-money-cant-buy)

Invariantes funcionales mínimos de toda épica (además de los del issue de auditoría): JSONs de raíz parsean; `py_compile` verde en los cuatro scripts; claves de `i18n.json` en pares `[en, es]` completos. Los workflows de producto (ADR-004) quedan fuera del ledger de valor: no son procesos del pipeline.
